{
  "name": "mini",
  "version": "1.0.0",
  "description": "Mini Program Engineering Configuration",
  "main": "src/app.js",
  "scripts": {
    "eslint": "eslint --ext .js,wxs src/ cloud/",
    "stylelint": "stylelint 'src/**/*.wxss'",
    "prettier": "prettier --check '*/**.{wxml,json}'",
    "prettier:fix": "prettier --write '*/**.{wxml,json}'",
    "lint": "npm run eslint && npm run stylelint && npm run prettier",
    "fix": "npm run eslint -- --fix && npm run stylelint -- --fix && npm run prettier:fix",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky install"
  },
  "dependencies": {},
  "devDependencies": {
    "babel-eslint": "^10.1.0",
    "eslint": "^7.32.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^4.0.0",
    "husky": "^7.0.2",
    "prettier": "^2.4.0",
    "stylelint": "^13.13.1",
    "stylelint-config-recommended": "^5.0.0"
  },
  "config": {},
  "keywords": [],
  "author": "zhilidali@qq.com",
  "license": "MIT"
}
